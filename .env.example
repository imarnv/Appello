# Overrides the voice bridge origin (no path, no trailing slash).
# Leave this unset in production: the site then uses the hosted bridge on Azure.
# Set it to ws://localhost:8000 to develop against backend/main.py.
NEXT_PUBLIC_VOICE_BRIDGE_URL=
